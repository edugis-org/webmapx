import type { IMap } from './IMapInterfaces';

/**
 * Backward-compatible alias for the legacy adapter name.
 * Prefer importing IMap from IMapInterfaces.ts in new code.
 */
export type IMapAdapter = IMap;
