# Tool: routing

Route planning between two map points. Draws the route and shows distance and estimated travel time. Uses a configurable routing service.

**Tool id:** `routing`  
**Load:** lazy

## Example

```js
tools: ['routing']
```

Requires a routing service API key in the app config. See the config JSON for your deployment.

← [All tools](./overview.md)
